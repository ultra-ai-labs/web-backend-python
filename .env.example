## MediaCrawler-main example environment file
# Copy this to `.env` and fill values before running the server.

# Flask server binding
FLASK_HOST=0.0.0.0
FLASK_PORT=8001

# Database (used by `db.py`). Example: mysql://root:password@127.0.0.1:3306/media_crawler
RELATION_DB_URL=mysql://root:698698@127.0.0.1:3307/media_crawler

# Qiniu (七牛云) storage credentials
QINIU_ACCESS_KEY=<your_qiniu_access_key>
QINIU_SECRET_KEY=<your_qiniu_secret_key>
QINIU_BUCKET_NAME=<your_qiniu_bucket>
QINIU_CDN_DOMAIN=<your_qiniu_cdn_domain_or_bucket_domain>

# Tencent COS storage credentials (optional if you use Qiniu)
TENCENT_SECRET_ID=<your_tencent_secret_id>
TENCENT_SECRET_KEY=<your_tencent_secret_key>
TENCENT_BUCKET_NAME=<your_tencent_bucket>
TENCENT_CDN_DOMAIN=<your_tencent_cos_cdn_domain>
TENCENT_REGION=<your_tencent_region>

# OpenAI / LLM keys
OPENAI_API_KEY=<your_openai_api_key>
DEEPSEEK_API_KEY=<your_deepseek_api_key>

# Analysis / thread settings
ANALYSIS_THREAD_NUM=6

# Application runtime defaults (override defaults from config/base_config.py)
SAVE_DATA_OPTION=db
PLATFORM=xhs
LOGIN_TYPE=qrcode
HEADLESS=true
ENABLE_IP_PROXY=false
IP_PROXY_PROVIDER_NAME=kuaidaili
USER_DATA_DIR=%s_user_data_dir

# Agent / marketing integration (if using `market-master` or remote agents)
REQUEST_URL=http://localhost:8001
AGENT_MODE=poll
AGENT_TOKEN=<shared_agent_token_if_used>

# Optional: enable debug mode
FLASK_DEBUG=false

# Notes:
# - Do NOT commit your real secrets to version control. Keep a private `.env` with real values.
# - `main.py` and other modules call `load_dotenv()` so values from `.env` will be loaded automatically.
JWT_SECRET_KEY=a-very-strong-secret-key-123456

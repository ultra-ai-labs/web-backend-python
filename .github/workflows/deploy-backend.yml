name: Deploy Backend

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT != '' && secrets.DEPLOY_PORT || '22' }}
          script: |
            export PROJECT_DIR="${{ secrets.DEPLOY_PROJECT_DIR }}"
            export BRANCH="main"
            chmod +x "${PROJECT_DIR}/scripts/deploy_backend.sh"
            "${PROJECT_DIR}/scripts/deploy_backend.sh"
